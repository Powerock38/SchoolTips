<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.schooltips">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SchoolTips">
        <activity android:name=".CultureActivity"></activity>
        <activity android:name=".CultureParamActivity" />
        <activity android:name=".ChoixActivity" />
        <activity android:name=".ResultatsActivity" />
        <activity android:name=".CreerCompteActivity" />
        <activity android:name=".CalculsParamActivity" />
        <activity android:name=".CalculsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>